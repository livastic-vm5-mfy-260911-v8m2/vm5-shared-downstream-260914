merge_queue:
  max_parallel_checks: 1
  mode: serial
queue_rules:
  - name: vm5-fork-rebase-boundary
    queue_conditions: true
    merge_conditions: true
    batch_size: 1
    max_checks_retries: 0
    update_method: rebase
    merge_method: merge
